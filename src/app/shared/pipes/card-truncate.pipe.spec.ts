import { CardTruncatePipe } from './card-truncate.pipe';

describe('CardTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new CardTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
